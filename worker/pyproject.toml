[project]
name = "cp-trainer-worker"
version = "0.1.0"
description = "CP Trainer sync + analytics worker (Codeforces mirror, mastery computation, usaco.guide seed)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "psycopg[binary]>=3.2",
    "httpx>=0.27",
]
